// @flow
import * as React from 'react';
import Button from 'Button.gen';

type Props = {};

class Test extens React.PureComponent < Props > {
  render() {
    return (
      <Button bsStyle='success' />
    );
  }
}
